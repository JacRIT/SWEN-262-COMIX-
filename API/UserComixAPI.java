public class UserComixAPI implements ComixAPI {
    ComicController ComixController;
    UserController UserController;
    UserComixAPI userComixAPI;

    /**
     * TODO : what this actually does.
     * @param username
     */
    login(String username);

}
